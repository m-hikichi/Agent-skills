# Shell scripts MUST stay LF on every platform. With core.autocrlf=true a
# Windows checkout would otherwise rewrite them to CRLF, and `bash script.sh`
# then fails ($'\r' / bad interpreter). The marp-slide Stop-hook completion
# gate (scripts/review-gate.sh) depends on this.
*.sh text eol=lf
